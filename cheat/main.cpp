#include "includes.h"
#include "../dependencies/drawing/imguii.h"



int main()
{
	auto mem = Memory("cs2.exe");

	const auto client = mem.GetModuleAdress("client.dll");

   render.overlay(features::run);

}
