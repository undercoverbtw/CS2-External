#pragma once


namespace config 
{
// menu & globals
	bool show_menu = true; // not done


// aimbot 

	bool aimbot = false; // not done
	bool show_aimbot_fov = false; // not done

	float aimbot_fov = 0; // not done
	float aimbot_smoothness = 0; // not done
	float aimbot_recoil_x = 0; // not done
	float aimbot_recoil_y = 0; // not done
	

// visuals
	bool visuals_box = false;
	bool visuals_health = false;
	bool visuals_health_text = false;
	bool visuals_name = false;
	bool visuals_box_cornered = false;
	bool viuals_skeleton = false;
	bool visuals_weapon = false;
	bool visuals_armor = false;
	bool visuals_visible_check = false;
	bool visuals_head_circle = false;
	bool visuals_head_line = false;


// miscellanous
	bool disable_team = false; // not done
	bool spectator_list = false; // not done
	bool hit_marker = false; // not done
	bool hit_sound = false; // not done


};